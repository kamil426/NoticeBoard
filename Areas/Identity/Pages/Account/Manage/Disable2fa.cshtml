@page
@model Disable2faModel
@{
    ViewData["Title"] = "Dezaktywacja weryfikacji dwuetapowej (2FA)";
    ViewData["ActivePage"] = ManageNavPages.TwoFactorAuthentication;
}

<partial name="_StatusMessage" for="StatusMessage" />
<h3>@ViewData["Title"]</h3>

<div class="alert alert-warning" role="alert">
    <p>
        <strong>Ta akcja tylko dezaktywuje 2FA.</strong>
    </p>
    <p>
        Dezaktywacja 2FA nie zmieni kluczy używanych w uwierzytelniających aplikacjach. Jeśli chcesz zmienić klucz używany
        w uwierzytelniających aplikacjach, to powinnieneś <a asp-page="./ResetAuthenticator">zresetować swoje klucze uwierzytelniające.</a>
    </p>
</div>

<div>
    <form method="post">
        <button class="btn btn-danger" type="submit">Dezaktywuj 2FA</button>
    </form>
</div>
